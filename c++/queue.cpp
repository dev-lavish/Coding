#include <iostream>
#include <queue>
using namespace std;
int main(){
    //first in first out
    queue <int> arr;
    arr.push(10);
    arr.push(20);
    arr.push(30);
    arr.push(40);
   //we cannot use iterator inn queue for traversing
    }
